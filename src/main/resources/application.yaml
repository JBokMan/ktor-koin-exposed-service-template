ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080

kafka:
    topic: "my-topic"
    bootstrapServers: "localhost:9092"
    groupId: "ktor-kafka-group"
    autoOffsetReset: "earliest"
    acksConfig: "all"

